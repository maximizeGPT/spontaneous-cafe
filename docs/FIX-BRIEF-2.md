# Fix brief 2 (design, copy, IA, conversion audit). Read docs/AUDIT-2-FINDINGS.md for evidence.

Shared contract. Every agent implements its side of these names exactly.

## New CSS (owner: the CSS/JS/build agent)
- `.hero--plain h1, .hero--plain .lede, .hero--plain .eyebrow, .hero--plain .hand { text-shadow: none }`
- `.day--five` : five columns at >= 900px (two columns 520 to 900, one below).
- `.price--wide` : a .price that spans the full grid row (`grid-column: 1 / -1`) with its body in two columns at >= 700px (amount + h3 + p on the left, ul + button on the right). Used by the catering "Larger events" card.
- `.photo-grid .span-2 { grid-column: span 2 }` (class now lands on the `<picture>` too), `.photo-grid .span-2 img { aspect-ratio: 2 / 1 }`, and at <= 600px `.photo-grid .span-2 { order: -1 }`.
- `.menu__stamp` restyled: Source Sans, uppercase, letter-spaced, red, small pill, still rotated slightly. No Caveat outside `.hand`.
- `.faq { max-width: var(--measure) }`; pages drop the ad hoc `measure` class on .faq.
- `.split:has(> .form) { align-items: start }` at all widths.
- `.tabs` at <= 600px: `display: grid; grid-template-columns: repeat(2, minmax(0,1fr))`; buttons padding-inline 0.5rem.
- `.site-footer .tagline` at <= 600px: `font-size: var(--text-xs); letter-spacing: 0.12em`.
- `.hero__toggle` at <= 760px: top-right under the header (`top: var(--space-4); bottom: auto`), 44px circle, label in `.visually-hidden` (JS adds the span with that class); remove the extra `.wrap` bottom padding rule.
- `@media (max-width: 400px) { .hero__actions .btn { padding-inline: 1rem } }`.
- `.hero__note a { color: inherit; text-decoration: underline; text-underline-offset: .2em }`.
- `.contact-aside .rates p { font-size: var(--text-sm) }` (just make sure `.contact-aside` handles a fourth block).

## build.py (owner: the CSS/JS/build agent)
- In `responsive_images`, move the img's `class` attribute onto the `<picture>` too (both keep it), so `.ph` and `.span-2` work on the wrapper; CSS gets `picture.ph { display: block; position: relative }` (other pictures stay `display: contents`).
- Review markup: unless the environment variable `REVIEW=1` is set, the build (a) removes every `<span class="tag-confirm">...</span>` and (b) removes the class `ph` from every element. Production on Vercel builds without REVIEW, so visitors never see review markup; `REVIEW=1 python3 build.py` keeps it for internal review. Document in README.

## site.js (owner: the CSS/JS/build agent)
- Contact form: prefill `select[name="tier"]` from `?tier=` the same way as `service` (only when a matching option exists). Include the tier in `_subject`: `Inquiry: <service>, <tier> from <name>` when a tier is chosen.
- Error copy: `That did not send. Email chefmattsamuelson@gmail.com or call (707) 972-6647.` with the phone as a `tel:` link inside `.form__status` (set innerHTML for this one case).
- Hero toggle: label text goes in a `<span class="visually-hidden">` plus an inline SVG pause/play glyph (two bars / triangle) so the chip can be a 44px circle on mobile; `aria-pressed` and the label text still toggle.

## Pages (owners: four page agents, same split as before)
Global rules for every service page (foraging, private-chef, catering, cooking-classes):
1. `<section class="section section--deep" id="prices">` on the Prices section, and MOVE the Prices section to sit directly after the "how it works / what the day is" section and BEFORE the seasonal sample-menu tabs.
2. Hero note on every service page, identical shape: `<p class="hero__note"><a href="#prices">3, 6 or 12 hours · From $300 · Ingredients at cost</a></p>` (catering: `3, 6 or 12 hours · From $300 · Larger events quoted`). Home hero note: `Any service · 3, 6 or 12 hours · From $300`. Private chef and classes hero secondary button becomes `<a class="btn btn--light" href="#prices">See the three lengths</a>`; foraging keeps "Other excursions" (#more); catering keeps "Smaller than twelve" pointing at /private-chef/#prices; Home keeps "Go foraging".
3. Tier card buttons: label `Ask about 3 hours` / `Ask about 6 hours` / `Ask about 12 hours` (numerals), href `/contact/?service=<service>&tier=3%20hours` etc. (tier values exactly: `3 hours`, `6 hours`, `12 hours`, `Larger event`). Catering "Larger events" card: class `price price--wide price--confirm`, amount `<p class="price__amount">Quoted</p><p class="small">per event</p>`, h3 `Larger events`, keep the five drivers, button `Get a quote` with `&tier=Larger%20event`. Put the cut on the cards: catering tier h3s become `3 hours · up to about twelve guests` etc. with a `<!-- CONFIRM: twelve-guest cut -->`; the wide card h3 `Larger events · above about twelve guests, or any event with rentals`.
4. `.cta` h2 per page: Home and Contact "Tell Matt the date and who's coming." Foraging "Tell Matt the day and how many are walking." Private chef "Tell Matt the night and the head count." Catering "Send Matt the date, the venue and a rough count." Classes "Tell Matt the kitchen and the date." Every CTA button label "Send an inquiry". Hero primary buttons: "Plan a dinner" (home, private chef), "Plan a day" (foraging), "Plan an event" (catering), "Plan a class" (classes).
5. Repetition budget across the site (each page agent trims its own pages to this): the honor-box farm stand: About sourcing (once) and Foraging day step (once) only, nowhere else, and no menu course notes mention it. "Thirty years": Foraging lede and the safety FAQ answer only; Home and Classes name the schools without year counts; About keeps the full count. The free town weekend: About (plain sentence, year once confirmed) and Catering as a scale reference only ("largest job: a free weekend feeding the town of Mendocino"); cut from Home. Engagement-trip story: Home .day list only; Foraging keeps one sentence pointing at Home's day? No: Foraging keeps the story in its opening, Home's .day list stays but ends with `<a class="link-arrow" href="/foraging/#prices">The twelve-hour day, $1,000 for the group</a>`; remove the tense shift (all past tense, third person).
6. Aphorisms and closers listed in AUDIT-2-FINDINGS.md (About 40 and 58, Home 35 and 79, Foraging 31 and 215, Classes 30 and 125, Catering 29) are cut or replaced with the fact behind them. Sentences that only sound good (Classes 51, Catering 28/30/48/70, Private chef 62, About 9, Home 35): replace with a checkable fact or cut. Vary list counts away from three.
7. Sub-experience cards (.sub) each get a `<a class="link-arrow" href="/contact/?service=Forest%20bathing%20or%20farm%20tour">Ask about a forest or farm day</a>` (dinner party: `service=Interactive%20dinner%20party`). Interactive dinner party is described on cooking-classes only; foraging's third .sub becomes a link-arrow to /cooking-classes/#more-party? Simpler: foraging keeps three subs but the dinner-party one is two lines plus the link to /cooking-classes/.
8. Cross-links: foraging 12-hour card gets `<a class="link-arrow" href="/private-chef/">How the dinner works</a>`; classes "Cook what you found" sub gets a link-arrow to /foraging/; catering Good-to-know gets a `Retreats` row linking to /cooking-classes/.
9. Catering hero gets `hero--short`.
10. Private chef: `ol class="day day--five"`; "Who books it" .grid-2 becomes .subs with four .sub cards using existing placeholder images (svc-chef.jpg, table-set.jpg, plate.jpg, group-cooking.jpg, class ph); add a `.figure--wide` (coast.jpg, ph) above the .details section.
11. Prices lede on private chef, foraging and classes: keep "Ingredients billed at cost with the receipts, on top of the tier" as a `.small` line (no chip markup needed now that the build strips it; keep the `<!-- CONFIRM -->`). Catering adds the same line.
12. Home: `.section-head .lede` under "Four ways" states the ladder once: "Three lengths, the same price for any service: 3 hours $300, 6 hours $500, 12 hours $1,000. Ingredients billed at cost." Home service card for catering: "Above about twelve guests, or with rentals, it is quoted per event." Home "Where the food comes from" h2 becomes "Bought within an hour of the table, or picked on the way." (About keeps "Small farms, honor boxes, the forest and the shore.").
13. About: delete the "More / Matt to add" dl (keep a `<!-- CONFIRM: more teaching history -->` comment); the CTA link after the story goes to `/contact/` with no query; move the guest quote "Matt is an expert in foraging..." off About onto Foraging (replace Foraging's current quote), and About gets no quote band (or keeps the section with the town-weekend fact as plain text).
14. Contact: h1 "Send Matt the details." and delete the duplicate h2; add a fourth aside block `<div class="rates"><p class="eyebrow">Rates</p><p>3 hours $300 · 6 hours $500 · 12 hours $1,000, any service. Ingredients at cost. Weddings and larger events are quoted.</p></div>`; add the tier field after service: `<div class="field"><label for="tier">How long</label><select id="tier" name="tier"><option value="">Not sure yet</option><option value="3 hours">3 hours, $300</option><option value="6 hours">6 hours, $500</option><option value="12 hours">12 hours, $1,000</option><option value="Larger event">Larger event, quoted</option></select></div>`; Date becomes required and the textarea optional, relabelled "Where you're staying, or the venue, and anything else"; message help "Dietary needs, anything else"; FAQ answers in Matt's voice; lead-time answer adds "Weddings and retreats book months to a year out; the catering page has the detail."
15. 404: `Or say what you were looking <span class="hand">for.</span>`
16. Eyebrows: foraging "What the baskets hold", classes "What a class cooks" (no "we" in eyebrows).
17. Testimonial: Home keeps the guest quote; Private chef keeps its shortened version; Foraging takes the About quote; Catering and Classes add a one-line credential near the CTA instead of a quote (catering in the wide card: "Cooking on this coast since 2009. Largest job: a free weekend feeding the town of Mendocino." classes in the .cta paragraph: "Twenty-three years teaching, at Living Light and Esalen.").

## Round 3 additions (Mohammed's review, 2026-09-08). These override anything above that conflicts.
- Tone: the site reads as trying too hard. Plainer headings, fewer clever lines. A heading may simply name the section ("What a class covers", "How a dinner works", "Prices"). Kill "Four things, and none of them is a recipe." and any heading in that family (a count plus a twist).
- Caveat accent: REMOVE every `<span class="hand">` from every h1, h2 and CTA on every page. Headings are plain Lora. Do not accent a single word anywhere. (The CSS agent restyles `.menu__stamp` without Caveat, so Caveat is no longer used on the site; the CSS agent removes the Caveat preload and @font-face too.)
- Private chef page: too literal and step-by-step. Replace the five-step .day timeline with a short prose section (two paragraphs) under a plain heading "How a dinner works", and cut the "Who books it" section to one paragraph. No .day on this page.
- Menu timing: the line "Written the night before, sometimes the morning of" is NOT accurate. Remove it and every variant ("writes them the night before", "settled by the time he loads the car", "the morning of"). Replace with: the menu is agreed with the guest in advance and built from what the farms, the market and the forest have that week.
- Sourcing facts: Matt forages mushrooms, berries, greens and salt. He does NOT hunt, fish or butcher; meat and fish are bought. Remove "Whole vegetables and whole animals" and every whole-animal, nose-to-tail, "stock from bones" line on About and elsewhere. Remove "line-caught off Noyo Harbor" style claims about fish; say "rockfish from the harbor" or "fish from Noyo". Remove "grass-fed lamb from an inland ranch" origin claims unless phrased as bought ("lamb from a ranch inland" is fine as a purchase). Keep "whole foods" as a cooking approach.
- Figure captions: remove `<figcaption>` from every figure. The image does not need a description under it.
- Contact details: the CTA band on every page shows ONLY the button ("Send an inquiry"). Delete the "Or call ... · email" line from every `.cta`. Phone and email stay in the footer and on the Contact page. The home page must not repeat the phone or email outside the footer.
- Footer tagline: "Local · Organic · Wild" (drop Seasonal). Home hero note: the price line per item 2 above (no "Seasonal · Local · Organic · Wild").
- Overall length: cut each service page by roughly a fifth. Prefer deleting a whole restating paragraph over trimming words.
